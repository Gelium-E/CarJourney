specVersion: "v1beta"
serviceId: "CarJourney"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "CarJourney-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
